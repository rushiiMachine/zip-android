[package]
version = "1.0.0"
edition = "2021"
name = "ziprs"

[dependencies]
zip = { version = "0.5.13", features = ["deflate"], default-features = false}
jni = "0.19.0"
thiserror = "1.0.30"

[lib]
crate_type = ["cdylib"]
