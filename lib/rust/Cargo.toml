[package]
name = "ziprs"
version = "1.0.0"
edition = "2021"

[lib]
crate_type = ["cdylib"]

[dependencies]
zip = { version = "0.5.13", features = ["deflate"], default-features = false }
thiserror = "1.0.30"
jni = "0.19.0"
